import { c } from './../index';

describe('', ()=>{
    test('should create a new IoC-container', ()=> {
        console.log('running c');
        console.log(c);
        expect(0).toBe(0);
    });
});
